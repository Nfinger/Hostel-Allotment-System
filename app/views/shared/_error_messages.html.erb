<% if @border.errors.any? %>
  <div id="error-explanation">
    <div class="alert alert-danger">
      The form contain <%= pluralize(@border.errors.count,"error") %>
    </div>
    
    <% @border.errors.full_messages.each do |msg| %>
    <%= msg %>
    <% end %>
    </ul>
  </div>
<% end %>